{
  "name": "sveltekit-auth-play-2",
  "version": "0.0.1",
  "scripts": {
    "dev": "env-cmd svelte-kit dev",
    "build": "svelte-kit build",
    "preview": "svelte-kit preview",
    "lint": "prettier --ignore-path .gitignore --check --plugin-search-dir=. .",
    "format": "prettier --ignore-path .gitignore --write --plugin-search-dir=. ."
  },
  "devDependencies": {
    "@sveltejs/kit": "next",
    "prettier": "^2.4.1",
    "prettier-plugin-svelte": "^2.4.0",
    "svelte": "^3.42.6"
  },
  "type": "module",
  "dependencies": {
    "cookie": "^0.4.1",
    "dotenv": "^10.0.0",
    "env-cmd": "^10.1.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.0.13",
    "uuid": "^8.3.2",
    "validator": "^13.7.0"
  }
}
